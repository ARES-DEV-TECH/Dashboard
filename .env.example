# Copier en .env ou .env.local et renseigner les valeurs.
# Ne jamais committer .env ou .env.local (secrets).

# Base de données
DATABASE_URL=postgresql://user:password@host:5432/dbname
# En prod (Vercel) : utiliser le pooler (port 6543)
# DATABASE_POOLER_URL=postgresql://...?pgbouncer=true&connect_timeout=30&connection_limit=1

# Auth
JWT_SECRET=un-secret-fort-en-prod

# Emails – Resend (prioritaire) ou SMTP
# Resend (API HTTP, pas SMTP – RESEND_HOST n’est pas utilisé)
# RESEND_API_KEY=re_xxx
# RESEND_FROM_EMAIL=dashboard-no-reply@votredomaine.com

# SMTP (ex. Hostinger) – pas d'espaces avant/après les valeurs
# Si erreur 535 (auth failed) : réinitialiser le mot de passe du compte email dans Hostinger, ou essayer port 587
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_PORT=587
# SMTP_USER=votre-email@votredomaine.com
# SMTP_PASSWORD=votre-mot-de-passe
# SMTP_FROM=votre-email@votredomaine.com (expéditeur affiché)

# Optionnel : Sentry
# NEXT_PUBLIC_SENTRY_DSN=
# SENTRY_DSN=
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=
